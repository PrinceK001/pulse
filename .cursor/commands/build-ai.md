Build and verify the Python AI agent.

1. Change to `pulse_ai/`
2. Check if a virtual environment exists, create one if not
3. Run `pip install -r requirements.txt`
4. Run `python -m py_compile pulse_agent/agent.py` to verify syntax
5. If the install or compilation fails, analyze the errors and suggest fixes
6. If it passes, report success
